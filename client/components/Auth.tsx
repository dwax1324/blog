


const Auth: React.FC<{}> = () => {

    function handleKeydown(e) {
        console.log(e)
    
    }

    return (
        <div onKeyPress={handleKeydown}>
        
        </div>
    )

}

export default Auth;